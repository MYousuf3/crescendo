<?xml version="1.0" encoding="utf-8"?>
<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:id="@+id/poplayout"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:layout_gravity="center"
    android:foregroundGravity="center"
    android:gravity="center">

    <!-- ImageView for the background -->

    <!-- TextViews aligned next to the numbers -->

    <ImageView
        android:id="@+id/backgroundImage"
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        android:scaleType="centerCrop"
        android:src="@drawable/backgroundtemplate3" />

    <TextView
        android:id="@+id/popScoreText"
        android:layout_width="407dp"
        android:layout_height="58dp"
        android:layout_alignStart="@+id/quipText"
        android:layout_alignEnd="@+id/quipText"
        android:layout_alignBottom="@+id/quipText"
        android:layout_marginStart="-76dp"
        android:layout_marginEnd="-95dp"
        android:layout_marginBottom="190dp"
        android:fontFamily="@font/metropolis"
        android:gravity="center"
        android:text="Your Popularity Score:  "

        android:textAllCaps="false"
        android:textColor="#6D28EC"
        android:textSize="28sp"
        android:textStyle="bold|italic" />

    <TextView
        android:id="@+id/quipText"
        android:layout_width="wrap_content"
        android:layout_height="126dp"
        android:layout_alignParentStart="true"
        android:layout_alignParentTop="true"
        android:layout_alignParentEnd="true"
        android:layout_alignParentBottom="true"
        android:layout_marginStart="86dp"
        android:layout_marginTop="346dp"
        android:layout_marginEnd="90dp"
        android:layout_marginBottom="259dp"
        android:fontFamily="@font/metropolis"
        android:foregroundGravity="center"
        android:gravity="center"
        android:text="Genre"
        android:textColor="@color/nice_black"
        android:textSize="25sp" />

</RelativeLayout>
